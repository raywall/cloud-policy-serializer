package engine

// func (e *Executor) LoadPlugins(pluginDir string) {
// 	files, _ := os.ReadDir(pluginDir)
// 	for _, f := range files {
// 		plugin, _ := LoadPlugin(filepath.Join(pluginDir, f.Name()))
// 		e.plugins[plugin.Metadata().Name] = plugin
// 	}
// }
